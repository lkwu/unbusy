<!DOCTYPE html>
<html class="no-js" lang="zh-TW">
<head>

  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">

  <!-- ========== VIEWPORT META ========== -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0" />

  <title>Unbusy</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

  <!-- ========== META TAGS ========== -->
  <meta name="description" content="盲人按摩">
  <meta name="keywords" content="unbusy, massage, 不忙按摩">

</head>

<body>

<div id="top-header">
  <div class="container-fluid">
    <div class="row">
      <div class="col-xs-12">
        <div id="home-logo">
          <%= link_to( image_tag("top-header.png", :alt => "HOME LOGO"), welcome_index_path ) %>
        </div>

        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        </div>
        <div class="collapse navbar-collapse" id="myNavbar">
          <ul class="nav navbar-nav navbar-right">
            <li>
              <% if user_signed_in? %>
                <%= link_to "登出", destroy_user_session_path, :method => :delete %>
              <% end %>
            </li>
            <li>
              <% if current_user && current_user.admin? %>
                <%= link_to "管理員", welcome_index_path %>
              <% end  %>
            </li>
            <li>
              <%= link_to "服務說明", about_path %>
            </li>
            <li>
              <%= link_to "各方評價", evaluate_path %>
            </li>
            <li>
              <% if user_signed_in? %>
                <%= link_to "預約查詢", user_path(current_user) %>
              <% else %>
                <%= link_to "預約查詢", user_session_path(:look_bookings => true) %>
              <% end %>
            </li>
            <li>
              <%= link_to '預約按摩', new_booking_path, :class =>"header_booking" %>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>

<%= yield %>

<div id="footer">
  <div class="container-fluid">
    <div class="row">
      <div class="col-xs-12">
        <div class="please">
          <p>和我們一起支持社會，讓世界變得更好</p>
        </div>

        <div class="copyright">
          <p >&copy; 2016 Unbusy 不忙按摩</p>
        </div>
      </div>
    </div>
  </div>
</div>


</body>
</html>
